
dataloader:
  root: data/processed
  normalization: stats/opensim_sincos_vel_acc_tau_v1/pretrain_v1_train_clean.pt
  batch_size: 32
  num_workers: 4

  train:
    split_set: pretrain_v1
    split: train
    source_datasets: ["AMASS"]
  
  val:
    split_set: babel_official
    split: val
    source_datasets: ["AMASS"]
